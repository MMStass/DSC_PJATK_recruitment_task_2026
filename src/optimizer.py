import optuna
import pandas as pd
import features

def generate_processed_data_parquet(data: pd.DataFrame, columns: list) -> pd.DataFrame:




    #X, test = combined.loc[X.index], combined.loc[test.index]
    return